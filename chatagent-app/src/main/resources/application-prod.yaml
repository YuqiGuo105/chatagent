spring:
  jpa:
    show-sql: false
  datasource:
    hikari:
      maximum-pool-size: 20
  ai:
    mcp:
      client:
        enabled: false  # Tools are in-process; no external MCP servers on Railway

logging:
  level:
    root: WARN
    com.example.chatagent: INFO

server:
  port: 8080
